import { CSChannelTypes } from '../csChannelTypes.js';
var ChannelSIPEndpoint = /** @class */ (function () {
    function ChannelSIPEndpoint(uri, headers, username, password) {
        this.type = CSChannelTypes.SIP;
        this.uri = uri;
        this.headers = headers;
        if (username != null) {
            this.username = username;
        }
        if (password != null) {
            this.password = password;
        }
    }
    return ChannelSIPEndpoint;
}());
export { ChannelSIPEndpoint };
//# sourceMappingURL=channelSIPEndpoint.js.map