import { IConfig } from "./IConfig.js";
import { IBridge } from "../../IBridge.js";
export declare class Config implements IConfig {
    bridge: IBridge;
    instanceServiceName: string;
    applicationId: string;
    apiApplicationId: string;
    apiAccountId: string;
    instanceId: string;
    privateKey: string;
    debug: boolean;
    logsSubmission: boolean;
    appUrl: string;
    assetUrl: string;
    namespace: string;
    constructor(bridge: IBridge);
    getExecutionUrl(func: string, pathname?: string, queryParams?: Record<string, string>): string;
    constructDebugUrl(hostname: string, pathname: string, queryParams?: Record<string, string>): string;
    constructProdUrl(hostname: string, pathname: string, queryParams?: Record<string, string>): string;
    queryObjectToString(queryObject: Record<string, string>): string;
}
//# sourceMappingURL=config.d.ts.map