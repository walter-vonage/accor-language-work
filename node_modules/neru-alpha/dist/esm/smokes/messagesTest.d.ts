import { IBridge } from "../IBridge.js";
import { ISession } from "../session/ISession.js";
import { IMessages } from "../providers/messages/IMessages.js";
import { INeru } from "../INeru.js";
export declare class MessagesSmokeTests {
    bridge: IBridge;
    session: ISession;
    messages: IMessages;
    neru: INeru;
    healthChecksUrl: string;
    constructor();
    onMessage(callback: string, fromNumber: string, toNumber: string): Promise<void>;
    onEvent(callback: string, fromNumber: string, toNumber: string): Promise<void>;
    sendMessage(fromNumber: string, toNumber: string, message: string): Promise<void>;
    checkIncomingMessageAndHitHealthEndpointIfSuccess(message: string, expectedMessage: string, successPathname: string): Promise<void>;
    checkIncomingEventAndHitHealthEndpointIfSuccess(sessionId: string, status: string, successPathname: string): Promise<void>;
}
//# sourceMappingURL=messagesTest.d.ts.map